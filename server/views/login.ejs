<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>EJS</title>
    <style>
        html, body {
            margin:0;
            width:100%;
            height:100%;
            font-size: 16px;
        }

        .borderBox{
            box-sizing: border-box;
        }

        .grad1 {
            background-color: #1fc8db; /* For browsers that do not support gradients */
            background-image: linear-gradient(141deg, #9fb8ad 0%, #1fc8db 51%, #2cb5e8 75%);
            opacity:0.95;
        }

        .grad2 {
            background-color: #1fc8db; /* For browsers that do not support gradients */
            background-image: linear-gradient(50deg, #2cb5e8 0%, #1fc8db 50%, #9fb8ad 75%);
            opacity:0.95;
        }

        .navOptBG {
            background-color: #2ab5d3;
        }

        .themeColor {
            background-color: #f1f1f1;
        }
        
        .w100pc {
            width:100%
        }

        .h100pc {
            height:100%
        }

        .xCenter {
            display: flex;
            justify-content: center;
        }

        .yCenter {
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .xyCenter {
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .xyVerticalCenter {
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }

        .height40 {
            height: 40px;
        }

        .margin10 {
            margin:10px;
        }

        .bdr1 {
            border: 1px solid gray;
        }



    </style>
</head>
<body class="themeColor">
    <header style="height:10%; width:100%">
        <% include partial/header %>
    </header>


    <main class="xyVerticalCenter" style="height:80%; overflow-y:auto">
        <% if(typeof msgContent !== 'undefined') { %>
            <div id="msgView"
                 style="border-radius: 5%; background-color: #ffebcd;max-width:80%;
                  min-width: 300px; position: absolute;top:11%; padding:10px;">
                <div onclick="closeMsg(event)"><b>&times</b></div>
                <div>
                    <b>
                        <%= msgContent.msgTitle %>
                    </b>
                </div>
                <span style="font-size:12px">
                        <%= msgContent.msg %>
                </span>
            </div>
        <% } %>

        <% if(form === "signIn") { %>
        <%  include partial/signInForm %>
        <% } else if(form === "signUp") { %>
        <%  include partial/signUpForm %>
        <% } else if(form === "forgotPwd") { %>
        <%  include partial/forgotPwd %>
        <% } %>

    </main>

    <footer style="height:10%; width:100%">
        <% include partial/footer %>
    </footer>

</body>
<script>
    function closeMsg(e) {
        let elem = document.getElementById("msgView");
        return elem.parentNode.removeChild(elem)
    }
</script>
</html>